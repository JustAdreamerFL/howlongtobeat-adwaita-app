# AI Agent Rules Configuration
# This file defines rules and constraints for GitHub Copilot AI agents working on this repository

# Security scanning rules
security:
  # CodeQL scanning configuration
  codeql:
    # Do not run CodeQL security scanning
    enabled: false
    reason: "CodeQL scanning consistently times out on this repository. The long analysis time makes it impractical for development workflow."
    
# Additional rules and guidelines for AI agents
guidelines:
  # Instructions for AI agents working on this repository
  - "Do not attempt to run CodeQL security scanner as it always times out"
  - "Skip any CodeQL-related security checks during the development workflow"
  - "Focus on other security validation methods that complete in reasonable time"
